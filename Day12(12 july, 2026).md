# Day 12 – Introduction to NumPy

## Topics Covered
Google colab Link : https://colab.research.google.com/drive/1XN2jsbg5TrIUrx9HS_T0YqQarlK4omIk?usp=sharing

Today, I started learning Python libraries and began with **NumPy**, one of the most widely used libraries for numerical computing in Python.

### Concepts Learned

* Introduction to Python Libraries
* What is NumPy?
* Importing NumPy using `import numpy as np`
* Creating NumPy arrays
* One-dimensional and two-dimensional arrays
* Array properties:

  * `shape`
  * `dtype`
  * `ndim`
* Creating arrays using:

  * `np.array()`
  * `np.zeros()`
  * `np.ones()`
  * `np.full()`
  * `np.arange()`
  * `np.linspace()`
  * `np.identity()`
* Reshaping arrays using `reshape()`
* Array indexing and slicing
* Sorting arrays
* Difference between slicing and copying arrays
* Broadcasting
* Conditional filtering on arrays
* Aggregate functions:

  * `min()`
  * `max()`
  * `argmin()`
  * `argmax()`
  * `sum()`
* Mathematical operations:

  * `sqrt()`
  * `sin()`
* Generating random arrays using `np.random.randint()`

## Practice

* Created and reshaped NumPy arrays.
* Practiced indexing and slicing operations.
* Applied broadcasting and conditional filtering.
* Used aggregate and mathematical functions.
* Generated random arrays and explored their properties.

## Key Takeaways

* Learned the basics of the NumPy library.
* Understood how NumPy arrays differ from Python lists.
* Gained hands-on experience creating, manipulating, and analyzing arrays.
* Built a strong foundation for future topics in data analysis and machine learning.
