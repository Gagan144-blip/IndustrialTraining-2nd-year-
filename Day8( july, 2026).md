# Day 8 – Python `for` Loop

## Topics Covered
-Google Colab Link : https://colab.research.google.com/drive/1imGw1oCI04vpsB7tFYAizMs7LbDmgV4i?usp=sharing

Today, I learned about the `for` loop in Python and how it is used to repeat tasks efficiently. I also explored the `range()` function and practiced iterating through lists and strings.

### Concepts Learned

#### What are Loops?

* Purpose of loops
* Why loops are used to avoid repetitive code
* Common use cases of loops

#### `for` Loop

* Syntax of the `for` loop
* Iterating over sequences
* Using `for` loops with:

  * Lists
  * Strings
  * Tuples
  * `range()`

#### `range()` Function

* `range(stop)`
* `range(start, stop)`
* `range(start, stop, step)`
* Understanding that the `stop` value is not included
* Forward counting
* Skipping numbers
* Reverse counting using negative step

#### Looping Through Lists

* Iterating through list elements
* Performing operations on each element
* Using loops to process list data

#### Looping Through Strings

* Iterating through characters of a string
* Processing each character individually

## Practice

* Printed numbers using `for` loops.
* Used different forms of the `range()` function.
* Practiced forward and reverse counting.
* Iterated through lists and displayed each element.
* Iterated through strings and printed each character.
* Solved basic programming problems using `for` loops.

## Key Takeaways

* Understood how `for` loops simplify repetitive tasks.
* Learned to generate number sequences using `range()`.
* Gained confidence in traversing lists and strings using loops.
* Improved logical thinking through loop-based practice problems.
